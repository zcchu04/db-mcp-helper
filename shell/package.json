{
  "name": "db-mcp-helper-shell",
  "version": "0.0.0",
  "private": true,
  "description": "DB MCP Helper desktop shell (Tauri wrapper around the Java setup backend)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
